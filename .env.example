# Public, optional Shopify Dev Dashboard catalog identifier.
# VITE_ values are compiled into the browser bundle and must never contain secrets.
# Leave blank to use credential-free organic Global Catalog search only.
VITE_SHOPIFY_CATALOG_ID=
