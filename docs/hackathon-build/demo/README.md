# Clunk demo draft

`clunk-demo-draft.mp4` is a 45-second, 1440×900, captioned capture of the public Lovable build. It shows:

1. the five-second promise and four appliance categories;
2. the one-click Whirlpool dishwasher answer;
3. the exact `W11412291` part and external seller handoff;
4. the visible shared action history and current WebMCP tool inventory;
5. the real-model washer flow and deterministic smoke/burning-smell stop;
6. the 31-model, four-flagship close.

The draft was recorded in a credential-free headless browser, so it correctly displays Guided mode. For the final Devpost upload, use this as b-roll or reproduce the same beats in Chrome 149 with WebMCP testing enabled, add the natural-language agent exchange, narrate the supplied outline, and keep the finished video under three minutes.
